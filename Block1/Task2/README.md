Union работает как с типом int, так и с string, корректно обрабатывая данные;
intersection не срабатывает при передаче объекта, не наследующего интерфейс Iterator, так как счет происходит через foreach